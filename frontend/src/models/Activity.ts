export interface Activity {
    id: number;           // Assuming id is a number
    name: string;         // Assuming name is a string
    description: string;  // Assuming description is a string
    image_url?: string;   // Assuming image_url can be a string and may be optional
    times: string;        // Adjust the type of times as necessary
  }